import { AdditionalRequestOptions } from '../aldi';
import { AldiObject } from '../base/aldiObject';
import { ExtendedRecipeModel, RecipeQueryModel } from './recipeModel';
export interface RecipeOptions {
    amount?: number;
}
export declare class Recipe extends AldiObject {
    /**
     * Gets recipe from ID
     * @param recipeId Recipe ID, formatted as "rezepte/{category}/{slug}.json"
     */
    getRecipeFromId(recipeId: string, additionalRequestOptions?: AdditionalRequestOptions): Promise<ExtendedRecipeModel>;
    /**
     * Get recipes featured on the front page
     * @param options Query options
     * @param options.amount Amount of recipes to get (default 20)
     */
    getFeaturedRecipes(options?: RecipeOptions, additionalRequestOptions?: AdditionalRequestOptions): Promise<RecipeQueryModel>;
    /**
     * Get recipes from given recipe name (will return all of them)
     * @param recipeName Recipe name to search for
     */
    getRecipesFromName(recipeName: string, additionalRequestOptions?: AdditionalRequestOptions): Promise<RecipeQueryModel>;
}
